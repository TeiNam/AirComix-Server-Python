# Comix Server Docker Environment Configuration
# 이 파일을 .env로 복사하고 설정을 수정하세요

# =============================================================================
# 필수 설정 (Required Settings)
# =============================================================================

# 만화 디렉토리 (호스트의 절대 경로)
# Manga Directory (absolute path on host system)
MANGA_DIRECTORY=/path/to/your/comix

# =============================================================================
# 서버 설정 (Server Configuration)
# =============================================================================

# 서버 포트 (기본값: 31257)
COMIX_SERVER_PORT=31257

# 디버그 모드 (프로덕션에서는 false로 설정)
DEBUG_MODE=false

# 로그 레벨 (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# =============================================================================
# 선택적 설정 (Optional Settings)
# =============================================================================

# 설정 디렉토리 (선택사항)
CONFIG_DIRECTORY=./config

# 로그 디렉토리 (선택사항)
LOG_DIRECTORY=./logs

# =============================================================================
# 파일 필터링 (File Filtering)
# =============================================================================

# 숨김 파일 목록 (쉼표로 구분)
HIDDEN_FILES=.DS_Store,Thumbs.db,@eaDir,._.DS_Store

# 숨김 패턴 (쉼표로 구분)
HIDDEN_PATTERNS=__MACOSX,*.tmp,*.temp

# =============================================================================
# 지원 파일 형식 (Supported File Formats)
# =============================================================================

# 이미지 파일 확장자
IMAGE_EXTENSIONS=jpg,jpeg,png,gif,bmp,tif,tiff,webp

# 아카이브 파일 확장자
ARCHIVE_EXTENSIONS=zip,rar,cbz,cbr

# =============================================================================
# 성능 설정 (Performance Settings)
# =============================================================================

# 최대 이미지 크기 (바이트, 기본값: 10MB)
MAX_IMAGE_SIZE=10485760

# 캐시 크기 (항목 수)
CACHE_SIZE=100

# =============================================================================
# 인증 설정 (Authentication Settings)
# =============================================================================

# 기본 인증 활성화 여부 (true/false)
ENABLE_AUTH=false

# 기본 인증 사용자명 (ENABLE_AUTH=true인 경우 필수)
AUTH_USERNAME=

# 기본 인증 패스워드 (ENABLE_AUTH=true인 경우 필수)
AUTH_PASSWORD=

# 리소스 제한
MEMORY_LIMIT=1G
CPU_LIMIT=2.0
MEMORY_RESERVATION=512M
CPU_RESERVATION=1.0

# =============================================================================
# 로깅 설정 (Logging Configuration)
# =============================================================================

# 로그 파일 최대 크기
LOG_MAX_SIZE=10m

# 로그 파일 최대 개수
LOG_MAX_FILES=5

# =============================================================================
# 개발 설정 (Development Settings)
# =============================================================================

# 디버그 포트 (개발용)
DEBUG_PORT=5678

# =============================================================================
# 예제 설정 (Example Configurations)
# =============================================================================

# Linux/macOS 예제:
# MANGA_DIRECTORY=/home/user/comix
# MANGA_DIRECTORY=/Users/username/Documents/Comix

# Windows 예제 (WSL 사용시):
# MANGA_DIRECTORY=/mnt/c/Users/username/Documents/Comix

# Docker Desktop 사용시:
# MANGA_DIRECTORY=C:\Users\username\Documents\Comix

# 인증 활성화 예제:
# ENABLE_AUTH=true
# AUTH_USERNAME=admin
# AUTH_PASSWORD=MySecurePassword123!

# 보안 권장사항:
# - 패스워드는 최소 8자 이상, 복잡한 조합 사용
# - 프로덕션에서는 반드시 강력한 패스워드 설정
# - 환경 변수 파일(.env)은 Git에 커밋하지 말 것