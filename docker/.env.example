# Comix Server Docker Environment Configuration

# Manga Directory (absolute path on host)
MANGA_DIRECTORY=/path/to/your/manga

# Server Configuration
COMIX_SERVER_PORT=31257
DEBUG_MODE=false
LOG_LEVEL=INFO

# Optional: Custom Configuration Directory
CONFIG_DIRECTORY=./config

# Optional: File Filtering
HIDDEN_FILES=.DS_Store,Thumbs.db,@eaDir
HIDDEN_PATTERNS=__MACOSX

# Optional: Supported File Extensions
IMAGE_EXTENSIONS=jpg,jpeg,png,gif,bmp,tif,tiff
ARCHIVE_EXTENSIONS=zip,rar,cbz,cbr