# Comix Server 기본 설정 예제
# 이 파일을 .env로 복사하고 필요에 따라 수정하세요

# =============================================================================
# 기본 서버 설정
# =============================================================================

# 만화 컬렉션이 저장된 디렉토리 (절대 경로 권장)
COMIX_MANGA_DIRECTORY=/home/user/manga

# 서버가 바인드할 호스트 (0.0.0.0 = 모든 인터페이스, 127.0.0.1 = 로컬만)
COMIX_SERVER_HOST=0.0.0.0

# 서버 포트 (AirComix 기본값: 31257)
COMIX_SERVER_PORT=31257

# =============================================================================
# 로깅 설정
# =============================================================================

# 디버그 모드 (개발 시에만 true로 설정)
COMIX_DEBUG_MODE=false

# 로그 레벨 (DEBUG, INFO, WARNING, ERROR, CRITICAL)
COMIX_LOG_LEVEL=INFO

# =============================================================================
# 파일 필터링 설정 (선택사항)
# =============================================================================

# 숨길 파일명 (쉼표로 구분)
COMIX_HIDDEN_FILES=.DS_Store,Thumbs.db,@eaDir,._.DS_Store

# 숨길 디렉토리 패턴 (쉼표로 구분)
COMIX_HIDDEN_PATTERNS=__MACOSX,.git,.svn,node_modules

# =============================================================================
# 지원 파일 형식 (선택사항)
# =============================================================================

# 지원할 이미지 파일 확장자 (쉼표로 구분, 소문자)
COMIX_IMAGE_EXTENSIONS=jpg,jpeg,png,gif,bmp,tif,tiff

# 지원할 아카이브 파일 확장자 (쉼표로 구분, 소문자)
COMIX_ARCHIVE_EXTENSIONS=zip,cbz,rar,cbr

# =============================================================================
# 고급 설정 (일반적으로 수정할 필요 없음)
# =============================================================================

# 최대 워커 수 (프로덕션 환경에서만 사용)
# GUNICORN_WORKERS=4

# 워커당 최대 연결 수
# GUNICORN_WORKER_CONNECTIONS=1000